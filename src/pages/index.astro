---
import { Github, Linkedin, Stackoverflow, X } from 'simple-icons-astro';
import { Heading } from '~/components/park-ui/heading';
import { Text } from '~/components/park-ui/text';
import { Box, Center, Grid, HStack, Stack } from '~/styled-system/jsx';
import Layout from '../layouts/Layout.astro';

import { Image } from 'astro:assets';
import { GlobeIcon, MailIcon, MoonIcon, PhoneIcon } from 'lucide-solid';
import avatarImage from '~/assets/avatar.jpg';
import Experience from '~/components/Experience';
import Skills from '~/components/Skills';
import { Link } from '~/components/park-ui/link';
import { css } from '~/styled-system/css';

const iconMargin = 1;
---

<Layout title="Kevin Mui">
  <Center>
    <Box maxW="6xl" mt={16}>
      <Grid gridTemplateColumns="1fr 2fr" w="full">
        <Stack>
          <Box p={2} w="max" h="max" border="solid" borderColor="red.9" borderWidth={1}>
            <Box bg="black" p={5} borderRadius="xl">
              <Image
                src={avatarImage}
                alt="Kevin Mui"
                class={css({ h: '6rem', w: '6rem', clipPath: 'circle(50%)' })}
              />
            </Box>
          </Box>
          <Stack gap={0}>
            <Heading as="h1" size="4xl" fontWeight="bold">Kevin Mui</Heading>
            <Heading as="h2" size="lg" color="fg.muted"
              >Software Engineer / Full Stack Engineer</Heading
            >
          </Stack>
          <Stack gap={0}>
            <HStack gap={2} color="fg.muted" alignItems="center">
              <GlobeIcon size="0.75rem" />
              <Text color="fg.muted" size="sm"> Chicago, IL</Text>
            </HStack>
            <Link href="mailto:kevinmui1997@yahoo.com">
              <HStack gap={2} color="fg.muted" alignItems="center">
                <MailIcon size="0.75rem" />
                <Text size="sm" color="fg.muted">kevinmui1997@yahoo.com</Text>
              </HStack>
            </Link>
          </Stack>
          <HStack color="red.9">
            <MailIcon size="1.25rem" class={css({ m: iconMargin })} />
            <PhoneIcon size="1.25rem" class={css({ m: iconMargin })} />
            <Linkedin size="1.25rem" class={css({ m: iconMargin })} />
            <X size="1.25rem" class={css({ m: iconMargin })} />
            <Github size="1.25rem" class={css({ m: iconMargin })} />
            <Stackoverflow size="1.25rem" class={css({ m: iconMargin })} />
          </HStack>
          <Heading as="h1" size="4xl" fontWeight="bold">About</Heading>
          <Text size="sm" color="neutral.11">
            As a seasoned Software Engineer with 8 years of senior level experience, specializing in
            fintech, Java backend, and React frontend development, I have a proven track record of
            designing and implementing high-volume risk management applications. During my tenure at
            CME Group for over 5 years, I honed my skills in developing cutting-edge solutions on
            Google Cloud Platform (GCP). I bring a unique blend of technical expertise and
            industry-specific knowledge that enables me to drive innovation and deliver exceptional
            results.
          </Text>

          <HStack alignItems="center">
            <MoonIcon />
            <Text>Dark</Text>
          </HStack>
        </Stack>
        <Stack>
          <Experience client:load />
          <Skills client:load />
        </Stack>
      </Grid>
    </Box>
  </Center>
</Layout>
